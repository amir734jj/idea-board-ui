const initialState = {
  name: null,
  email: null,
  token: null,
  expiration: null,
};

export const userInfoReducer = (state = initialState, action) => {
  switch(action) {
    default:
      return state;
  }
};
